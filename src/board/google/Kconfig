if VENDOR_GOOGLE

choice
	prompt "Mainboard model"

config BOARD_GOOGLE_LINK
	bool "Link"

endchoice

source "src/board/google/link/Kconfig"

config MAINBOARD_VENDOR
	string
	default "Google"

endif # VENDOR_GOOGLE
